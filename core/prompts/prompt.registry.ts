import { TITLE_PROMPT } from "./title.prompt";
import { BULLET_PROMPT } from "./bullet.prompt";
import { SEO_PROMPT } from "./seo.prompt";
import { PRICING_PROMPT } from "./pricing.prompt";
import { DESCRIPTION_PROMPT } from "./description.prompt";

export const PromptRegistry = {
  title: TITLE_PROMPT,
  bullet: BULLET_PROMPT,
  seo: SEO_PROMPT,
  pricing: PRICING_PROMPT,
  description: DESCRIPTION_PROMPT,
};
