export type Product = {
  id?: string;
  title: string;
  price: number;

  cost?: number;

  sales?: number;
  sellers?: number;

  trendCurrent?: number;
  trendPast?: number;

  likes?: number;
  shares?: number;
  comments?: number;

  // ===== SCORING =====
  demandScore?: number;      // 0–100
  competitionScore?: number;  // 0–100
  marginScore?: number;      // 0–100
  viralScore?: number;       // 0–100

  // ===== FINAL DECISION =====
  score?: number;            // 0–100
  decision?: "auto-import" | "review" | "reject";
  cost?: number;

  sales?: number;
  sellers?: number;

  trendCurrent?: number;
  trendPast?: number;

  likes?: number;
  shares?: number;
  comments?: number;

  // ===== SCORING =====
  demandScore?: number;      // 0–100
  competitionScore?: number;  // 0–100
  marginScore?: number;      // 0–100
  viralScore?: number;       // 0–100

  // ===== FINAL DECISION =====
  score?: number;            // 0–100
  decision?: "auto-import" | "review" | "reject";  isWinning?: boolean;
};

  isWinning?: boolean;
};

