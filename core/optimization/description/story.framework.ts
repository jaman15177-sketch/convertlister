export class StoryFramework {
  build(title: string): string {
    return `
Many people struggle to find a reliable ${title} that truly improves their daily lifestyle.

This product is designed to bring more convenience, confidence, and efficiency into everyday use.
`;
  }
}
