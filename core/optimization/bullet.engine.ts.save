import { Product } from "../product.model";

export class BulletEngine {
  generate(product: Product) {
    return [
      `High quality ${product.title}`,
      "Trending product with strong demand",
      "Optimized for customer conversion",
    ];
  }
}
