export function shouldImport(score: number) {
  if (score >= 80) return "auto";
  if (score >= 60) return "review";
  return "reject";
}
const score = calculateWinningScore(product);

const decision = shouldImport(score);

if (decision === "auto") {
  // send to Shopify / Amazon
}

if (decision === "review") {
  // manual validation queue
}
ProductModel[]

nano core/scoring/ranking.ts


